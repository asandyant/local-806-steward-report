# Local 806 Steward Report App - Maintenance Mode

This version keeps the working steward report app code in the repo as:

`app-original-hidden.html`

The public bookmarked page uses `index.html`, which now shows a maintenance message:

"Profile Temporarily Unavailable"

To restore access later, copy `app-original-hidden.html` back over `index.html`, then commit and push.
